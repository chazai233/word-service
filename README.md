# Word Document Generation Service

FastAPI 服务，用于生成格式化的 Word 文档。

## 部署到 Railway

1. Fork 此仓库
2. 在 Railway 中选择 "Deploy from GitHub repo"
3. Railway 会自动检测并部署

## API 端点

- `POST /generate-from-template` - 生成 Word 文档
- `GET /health` - 健康检查
- `GET /docs` - API 文档
